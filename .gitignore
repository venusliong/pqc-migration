# third-party scan targets (shallow clones, re-fetch with git clone --depth 1)
targets/

# CodeQL databases (rebuild: codeql database create dbs/<name> --language=cpp --command=make)
dbs/
codeql/.codeql/

# local Claude Code permission settings
.claude/settings.local.json
